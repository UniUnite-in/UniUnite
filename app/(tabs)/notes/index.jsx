import { SafeAreaView, Text } from 'react-native';

const NotesScreen = () => {
  return (
    <SafeAreaView className="flex-1 items-center justify-center bg-white">
      <Text className="text-3xl font-bold text-black">Notes</Text>
    </SafeAreaView>
  );
};

export default NotesScreen;
